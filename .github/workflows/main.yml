name: My CICD pipeline in Github Actions

on: 
 push: 
  branches: main

jobs:
  build:
   runs-on: uubuntu-latest

   steps: 
    -  uses: actions/checkout@v4   

    -  name: My first step
       run:
         echo "This is a Devops"
         uname -a
       
      
